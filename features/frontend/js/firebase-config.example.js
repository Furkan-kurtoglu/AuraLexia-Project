/**
 * Firebase şablonu — bu dosyayı repoya gerçek anahtarlarla commit ETMEYİN.
 *
 * Kurulum:
 * 1. Bu dosyayı kopyalayın: firebase-config.local.js
 * 2. Aşağıdaki alanları Firebase Console web uygulaması yapılandırmasından doldurun.
 * 3. index.html içinde firebase-config.local.js yüklendiğinden emin olun (varsayılan akışta vardır).
 *
 * firebase-config.local.js gitignore içindedir; asla versiyon kontrolüne eklemeyin.
 */
window.AuraLexiaFirebaseConfig = {
  apiKey: '',
  authDomain: '',
  projectId: '',
  storageBucket: '',
  messagingSenderId: '',
  appId: '',
};
